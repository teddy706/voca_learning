# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# env
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# python (scripts/import_vocab_csv.py)
__pycache__/
*.pyc

# supabase
supabase/.temp
.vercel
.env*
!.env.local.example

# Claude Code runtime state (not project config)
.claude/scheduled_tasks.lock

# 단어 등록 원본 오디오(180MB) — git에 커밋하지 않는다. 검수된 리뷰 CSV(voca_mp3/*.csv, ~200KB)는
# 무엇을 가져왔는지 기록으로 남기기 위해 추적한다(scripts/import_vocab_csv.py의 입력).
voca_mp3/*.mp3
